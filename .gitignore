# Large installers / junk — never commit (this is what blocked the first push)
*.rpm
*.deb

# Upstream DLIO README media (gifs/images) — not needed to build or deploy.
# Delete this line if you want the full upstream docs in the repo.
ros2_ws/src/direct_lidar_inertial_odometry/doc/

# Python / pytest artifacts
__pycache__/
*.py[cod]
.pytest_cache/
